apiVersion: v1
kind: Secret
metadata:
  name: forge-secrets
  namespace: meshcue
  labels:
    app: forge
    app.kubernetes.io/name: forge
    app.kubernetes.io/part-of: meshcue
type: Opaque
data:
  # Base64-encoded placeholders — replace with real values before applying
  # echo -n "your-key" | base64
  ZOO_CAD_API_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
  LLAMA_MESH_ENDPOINT: "REPLACE_WITH_BASE64_ENCODED_ENDPOINT"
  HUNYUAN3D_ENDPOINT: "REPLACE_WITH_BASE64_ENCODED_ENDPOINT"
  COSMOS_ENDPOINT: "REPLACE_WITH_BASE64_ENCODED_ENDPOINT"
  POSTGRES_PASSWORD: "Zm9yZ2U="  # "forge" — change in production
